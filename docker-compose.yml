services:
  ecs-controller:
    image: kori1c/ecs-controller:latest
    container_name: ecs-controller
    restart: always
    ports:
      - "43211:80"
    volumes:
      - ./data:/var/www/html/data
    environment:
      - TZ=Asia/Shanghai
    dns:
      - 223.5.5.5
      - 114.114.114.114